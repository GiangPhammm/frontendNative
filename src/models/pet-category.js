export default class PetCategory {
    /**
     * @param {string} id 
     * @param {string} name 
     * @param {string} color 
     */
    constructor (id, name, color) {
        this.id = id;
        this.name = name;
        this.color = color;
    }
}